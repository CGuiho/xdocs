---
name: Global Skill Default and Discovery Trigger
purpose: Define how xdocs installs its bundled agent skill during initialization and when agents should load that skill.
description: Records the global-by-default initialization behavior, explicit local override, broad codebase-discovery triggers, and release validation requirements.
created: 2026-07-14
flags:
  - decision
  - superseded
tags:
  - cli
  - agents
  - documentation
keywords:
  - xdocs init
  - global skill
  - local skill
  - agent trigger
  - codebase discovery
  - context recommendations
owner: xdocs-decisions
---

# Global Skill Default and Discovery Trigger

> Superseded on 2026-07-18 by the approved RFC 0034 CLI migration. `xdocs init`
> no longer mutates agent files. Explicit `xdocs agent skill` actions default
> global, use `--local` for project scope, and always target both supported
> skill directories.

## Summary

The bundled `guiho-s-xdocs` skill must be available across projects because its
primary value includes helping agents search, navigate, and understand any
xdocs-enabled codebase. `xdocs init` will therefore install the skill globally
by default. A project-local installation remains available only when the user
explicitly requests `xdocs init --local`.

The skill activation metadata and the AGENTS.md section generated by xdocs must
also describe codebase discovery as a primary trigger, not merely descriptor
maintenance.

## CLI Behavior

- `xdocs init` installs or refreshes the bundled skill in the user-level agent
  skill directory.
- `xdocs init --global` remains supported as an explicit spelling of the
  default behavior.
- `xdocs init --local` installs or refreshes the skill beneath the initialized
  project.
- Passing `--local` and `--global` together fails with a clear usage error and
  performs no skill installation.
- `xdocs agents install local|global` retains its existing explicit behavior.
- Existing tool targeting and detection rules remain unchanged: `agents` is the
  standard target, while Claude-specific installation requires an explicit
  request or existing Claude project markers.

## Agent Skill Trigger

In an xdocs-enabled project, agents should load `guiho-s-xdocs` whenever the
user wants to:

- search for project information;
- understand a codebase, application, package, module, feature, or directory;
- locate relevant implementation files or documentation;
- determine the smallest useful reading set for a task;
- navigate project structure or inspect metadata; or
- create, update, validate, or maintain xdocs documentation.

The skill should direct discovery work through `xdocs context`, `xdocs meta`,
`xdocs tree`, and `xdocs list` before broad source or documentation reads.

## Implementation Surface

The change covers CLI flag parsing and help, `xdocs init`, bundled skill
frontmatter and instructions, generated AGENTS.md guidance, regression tests,
canonical user documentation, the changelog, and affected xdocs descriptors.
No change is required to the explicit `xdocs agents install` subcommand.

## Validation and Release

Regression tests must cover the global default, the explicit local override,
and conflicting scope flags. Release validation includes typecheck, all tests,
library build, native CLI compilation, and scoped xdocs health checks. After the
release documentation is current, GUIHO Mirror will apply the patch transition
from `0.5.2` to `0.5.3`.

## Rejected Alternatives

- Removing local installation entirely was rejected because an explicit
  project-isolated install remains useful.
- Keeping local installation as the default was rejected because it makes the
  discovery skill unavailable in other xdocs-enabled projects until each one is
  initialized independently.
- Changing only documentation was rejected because it would leave the CLI's
  incorrect default behavior intact.
